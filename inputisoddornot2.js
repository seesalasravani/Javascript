let isOdd = function(num) {
    if (num % 2 !== 0) {
      console.log(`${num} is odd`);
    } else {
      console.log(`${num} is even`);
    }
  }
  
  isOdd(11); // Output: 11 is odd
  isOdd(20); // Output: 20 is even